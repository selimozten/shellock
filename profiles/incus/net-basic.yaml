config:
  limits.cpu: "4"
  limits.memory: 8GiB
description: Basic network assessment profile
devices:
  eth0:
    name: eth0
    network: incusbr0
    type: nic
  root:
    path: /
    pool: default
    type: disk
name: net-basic
