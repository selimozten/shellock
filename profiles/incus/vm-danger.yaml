config:
  limits.cpu: "8"
  limits.memory: 16GiB
  security.secureboot: "false"
description: VM-oriented high-risk analysis profile; use only for authorized isolated lab work
devices:
  eth0:
    name: eth0
    network: incusbr0
    type: nic
  root:
    path: /
    pool: default
    type: disk
name: vm-danger
